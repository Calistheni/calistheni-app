import { NextResponse } from "next/server"; import { prisma } from "@/lib/prisma"; import { createUserUnauthorizedResponse, getAuthenticatedUserId } from "@/lib/user-auth"; import { toFoodSummary } from "@/lib/nutrition/service"; import { nutritionFoodVisibilityWhere } from "@/lib/nutrition/food-visibility";
const common = ["chicken breast", "rice", "egg", "banana", "apple", "oats", "greek yogurt", "potato", "broccoli", "salmon"];
export async function GET() { const userId = await getAuthenticatedUserId(); if (!userId) return createUserUnauthorizedResponse(); const foods = await prisma.food.findMany({ where: { AND: [{ normalizedName: { in: common } }, nutritionFoodVisibilityWhere(userId)] }, include: { aliases: { select: { name: true } }, details: { select: { categories: true, productImageUrl: true } }, servings: { select: { name: true, quantity: true, grams: true, householdUnit: true, isDefault: true } } }, take: 20 }); return NextResponse.json({ foods: foods.sort((a, b) => common.indexOf(a.normalizedName) - common.indexOf(b.normalizedName)).map(toFoodSummary) }); }
